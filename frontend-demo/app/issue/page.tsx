import * as React from 'react';

export default function IssuePage() {
  return (
    <div style={{padding:20}}>
      <h1>Issue Credential (Demo)</h1>
      <p>This page demonstrates issuing flow and will call the issuer backend via an API proxy.</p>
    </div>
  );
}
